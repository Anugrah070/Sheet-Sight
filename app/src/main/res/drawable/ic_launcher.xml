<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <!-- Background -->
    <path
        android:fillColor="#1C1B1F"
        android:pathData="M0,0h108v108h-108z" />

    <!-- Simple stylized eighth-note glyph standing in for real branding.
         Replace with the real app icon (and adaptive foreground/background
         layers) before release; this only needs to satisfy the manifest's
         android:icon / android:roundIcon references during development. -->
    <path
        android:fillColor="#D0BCFF"
        android:pathData="M62,24 L62,64 A14,11 0 1,1 58,63.5 L58,34 L78,28 L78,20 Z" />
</vector>
